# Dependencies
node_modules/

# Next.js build output
.next/
out/
build/
next-env.d.ts
tsconfig.tsbuildinfo

# Env
.env
.env*.local

# Build artifacts / masters - NOT served, and heavy.
# media-src holds the 45.7MB hero master and the ffmpeg intermediates.
media-src/
_ref/

# Video intermediates. The shipped clip (hero-new-720.mp4) and its poster ARE
# tracked - these are only the ffmpeg staging files and the HEVC master.
public/video/hero-new-fwd.mp4
public/video/hero-new-rev.mp4
public/video/Hero-section-new-video.mp4
public/video/list.txt

# Scratch / probe output. Written by terminal probes because PowerShell stdout
# comes back empty over the VS Code bridge, so results go to a file and get
# read back. None of it is source.
*.log
dev.log
tscheck*.txt
statuscheck*.txt
perfscan.txt
filecount.txt
blackscan.txt
fontrefs.txt
fontaudit.txt
syncscan.txt
syncscan.ps1
gitcheck.txt
gitfiles.txt
gitinit.txt
probe.txt
cornerrun.txt
cornerAvg.txt
cornerMax.txt
lumarun*.txt
lumadata*.txt
transcode.txt
videolist.txt
boomerang.bat
boomerang.log
download-fonts.ps1
fontdl.log
patch-perf.ps1

# Local-only MCP guide (details are served by the MCP itself via get_usage_guide)
docs/MCP-DOC.md

# OS
.DS_Store
Thumbs.db

# Probe and log artifacts from local debugging sessions. Kept on disk, never committed.
/*.txt
!/*.example.txt
